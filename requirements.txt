# Enhanced Blender Render Pipeline - Requirements
# Python 3.8+ required

# Core GUI dependencies
tkinter-dev>=0.1.0; sys_platform == "linux"
Pillow>=10.0.0
psutil>=5.9.0

# Numerical computing (Numba優先)
numpy>=1.21.0
numba>=0.58.0

# Computer vision and image processing
opencv-python>=4.8.0
scikit-image>=0.21.0
imageio>=2.31.0

# Machine Learning / AI (CUDA対応)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# CUDA support (GPU acceleration)
cupy-cuda12x>=12.0.0; sys_platform != "darwin"

# Progress bars and utilities
tqdm>=4.65.0

# Logging and debugging
colorama>=0.4.6
rich>=13.5.0

# File format support
imageio-ffmpeg>=0.4.8

# Optional dependencies for enhanced features
# Real-ESRGAN support
basicsr>=1.4.2; extra == "ai"
facexlib>=0.3.0; extra == "ai"
gfpgan>=1.3.8; extra == "ai"

# RIFE support  
kornia>=0.7.0; extra == "ai"

# Intel OIDN Python bindings
oidn>=2.0.0; extra == "denoise"

# Development dependencies
pytest>=7.4.0; extra == "dev"
pytest-cov>=4.1.0; extra == "dev"
black>=23.7.0; extra == "dev"
isort>=5.12.0; extra == "dev"
flake8>=6.0.0; extra == "dev"
mypy>=1.5.0; extra == "dev"
pre-commit>=3.3.0; extra == "dev"

# Cython for performance optimization
Cython>=3.0.0; extra == "cython"

# Build tools
setuptools>=65.0.0
wheel>=0.41.0
build>=0.10.0
